services:
  tse:
    build: .
    image: tse-modernization:latest
    command: ["serve", "--db", "/app/Data/tse.db", "--web", "/app/web", "--port", "8888"]
    ports:
      - "8888:8888"
    volumes:
      - ./Data:/app/Data
    restart: unless-stopped
